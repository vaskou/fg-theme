# Plugins

- You may use any of the installed Claude Code plugins (MCP servers, skills, etc.) at any time when you judge that doing so would be useful for the task — no need to ask first.
- Still respect other project rules: clean up any artifacts the plugin produces (e.g. Playwright output per `playwright-artifacts.md`) before ending the task.
